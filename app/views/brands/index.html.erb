<h1 align="center"> Welcome To Brands Page </h1>


  <div class="row" align="center" >
    <td><%= link_to('Create New Brand', new_brand_path , method: :get, class: "btn btn-large btn-primary")%></td>

  </div>

<div id="brands" class="tab-pane">
    <div class="table-responsive">
      <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Id</th>
          <th>Name</th>
          <th>Action</th>
        </tr>
      </thead>

      <tbody>
        <% @brands.each_with_index do |brand,index| %>
          <tr>
            <td><%= brand.id %></td>
            <td><%= brand.name %></td>
            <td><%= link_to('Edit', edit_brand_path(brand), class: "btn btn-large btn-primary")%></td>
            <td><%= link_to('Delete', brand_path(brand), method: :delete, class: "btn btn-large btn-primary")%></td>
            <!-- <td><%#= image_tag user.image_url, class: "image_table_display"%></td> -->
            <%#= render :partial => 'images/image', :locals => {:image => image} %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
